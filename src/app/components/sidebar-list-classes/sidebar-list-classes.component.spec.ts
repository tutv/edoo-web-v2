/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SidebarListClassesComponent } from './sidebar-list-classes.component';

describe('Component: SidebarListClasses', () => {
  it('should create an instance', () => {
    let component = new SidebarListClassesComponent();
    expect(component).toBeTruthy();
  });
});
